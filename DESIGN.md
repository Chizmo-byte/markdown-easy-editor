# DESIGN.md — LifeMargin デザインシステム

LifeMargin の UI 作業は必ずこのファイルのトークンと原則に従うこと。
Notion（温かみ・余白・1色のアクセント規律）と Wise（大きな角丸・親しみやすいグリーン・
コントラストで生む奥行き）を下敷きに、「人生の余白」を感じられる落ち着いた印象を目指す。

参照元: `NotionDesign.md` / `WiseDesign.md`

---

## ブランドの方向性

- **温かみのあるオフホワイトの紙**の上に置かれた document のような佇まい（Notion）。
- **大きく丸い角**と**たった1色の親しみやすいグリーン**で、やわらかいフィンテックの声（Wise）。
- 重要な見出しだけ**美しいセリフ体（明朝）**で語らせ、本文・UI はニュートラルなサンセリフ。
- 無機質な均等グリッドを避け、**たっぷりの余白**と角丸で「ホッとする」レイアウトにする。

---

## カラートークン

Tailwind v4 の `@theme`（`globals.css`）で定義。クラス名は `bg-canvas` / `text-ink` 等。

| トークン | 値 | 用途 |
|---|---|---|
| `canvas` | `#faf9f6` | ページ背景（温かいオフホワイト・紙のような地）。clinical な純白は使わない |
| `surface` | `#ffffff` | カード・パネル・入力欄。canvas との明度差で奥行きを作る |
| `ink` | `#1d1c1a` | 見出し・本文の基本色（わずかに暖かいニアブラック） |
| `ink-secondary` | `#54524d` | 副次的な本文 |
| `ink-muted` | `#8a857d` | キャプション・プレースホルダー |
| `hairline` | `#ebe8e2` | 1px の境界線・区切り |
| `accent` | `#9fe870` | **唯一の構造アクセント**。主要 CTA の塗り（Wise グリーン） |
| `accent-hover` | `#92dd5f` | アクセントの hover/active 状態 |
| `accent-ink` | `#163300` | グリーンのピル上に乗せる濃い森林グリーンの文字 |
| `accent-soft` | `#eef7e4` | 淡いグリーンの面（バッジ背景・フィロソフィー帯など） |

### アクセント運用ルール（最重要）
- グリーンは **1色だけ**。第2のブランドアクセントを足さない。
- グリーンは**主要 CTA の塗り**と**淡い面（accent-soft）**にのみ使う。装飾的に散らさない。
- グリーンのピルには `accent-ink`（濃緑）の文字を乗せる。グリーン地にグリーン文字は禁止。
- ステータス（成功/警告/エラー）にブランドグリーンを流用しない（緑は CTA の色）。

---

## タイポグラフィ

| 役割 | フォント | 使いどころ |
|---|---|---|
| ディスプレイ（見出し） | **Shippori Mincho**（明朝・セリフ） | Hero / セクション見出し / ロゴワードマーク。`font-serif` |
| 本文・UI | Geist Sans（ニュートラルなサンセリフ） | 本文・ボタン・ナビ。既定 `font-sans` |
| エディタ・コード | Geist Mono | 入力欄・ソースプレビュー。`font-mono` |

### 原則
- 重要な見出しは**セリフ（明朝）**で、`tracking-tight` 気味に締めて落ち着いた品位を出す。
- 本文は weight 400、見出しのセリフは 600/700。重さのコントラストで階層を作る。
- セリフは「重要な見出し」限定。本文・キャプション・ボタンには使わない。

---

## 角丸（Radius）

| 用途 | Tailwind | 値 |
|---|---|---|
| CTA・主要ボタン・セグメント | `rounded-full` | フルピル |
| カード・大きな面 | `rounded-3xl` | 24px（Wise の標準カード半径） |
| 中サイズの面・入力欄 | `rounded-2xl` / `rounded-xl` | 16 / 12px |
| ピルバッジ | `rounded-full` | — |

シャープな角は UI に使わない。やわらかさがブランドの親しみやすさ。

---

## 余白・レイアウト

- セクションは大きな縦の余白（`py-20`〜`py-28`）で区切る。罫線より余白で grouping。
- コンテナは中央寄せ・広め（`max-w-5xl` 前後）、外側ガターをたっぷり。
- カードは canvas の上に surface（白）を置き、**面の明度差**で浮かせる（Wise）。影は控えめ。
- 均等な格子の連続を避け、見出し・本文・CTA の縦リズムと余白で「開放感」を出す。

## 奥行き（Elevation）

- 既定はフラット（hairline のみ）。Notion 流の「ほとんど影がない」レイヤード・シャドウ。
- hover でごく淡い影（`hover:shadow-md` 程度）。重いドロップシャドウは使わない。
- 主たる奥行きの手がかりは canvas↔surface の面コントラスト。

---

## コンポーネント指針

- **主要 CTA**: `bg-accent text-accent-ink rounded-full`、`hover:bg-accent-hover`、`active:scale-95`。
- **副次ボタン / ユーティリティ**: `bg-surface text-ink border border-hairline rounded-full`。
- **セグメントコントロール**: `rounded-full` のピル群。選択中は `bg-ink text-surface`（ニュートラル）。
- **カード**: `bg-surface rounded-3xl border border-hairline`、内側 `p-6`〜`p-8`、hover で淡い影。
- **淡いグリーンの面**: `bg-accent-soft`（フィロソフィー帯・バッジ等）。
- **入力欄**: `bg-surface border border-hairline rounded-xl`。角はピルにしない。

---

## Do / Don't

### Do
- 背景は温かい `canvas`。カードは `surface` で図と地をやさしく分ける。
- 重要な見出しはセリフ（Shippori Mincho）で品よく。
- グリーンは主要 CTA と淡い面にだけ、効果的に1色。
- 角は大きく丸める（カード 24px・ボタンはピル）。

### Don't
- 純白の clinical な全面背景にしない（温かい canvas が核）。
- グリーンを装飾的に散らさない／第2アクセントを足さない。
- 本文・ボタンをセリフにしない（セリフは重要見出し限定）。
- シャープな角・重い影を使わない。
